[project]
name = "sdd-tw-examples"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "behave>=1.3.3",
    "isort>=8.0.1",
    "pytest>=9.0.3",
]

[dependency-groups]
dev = [
    "ruff>=0.15.13",
]
